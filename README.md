# Diagnost API

## 🚀 Getting Started

Follow these steps to get the project up and running.

### 1. Configuration

Create a `.env` file based on the provided example:

```bashcp
cp example.env .env
```

> **Note:** Make sure to update the variables in `.env` with your specific configuration.

### 2. Run

Start the application in detached mode:

Bash

```
docker-compose up -d
```
